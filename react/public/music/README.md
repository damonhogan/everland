This folder is intended to hold audio assets used by the GM Dashboard and scenes.

Place .mp3 or .ogg files here. Filenames referenced by `public/bbs/audio_assets.json` must match the files under this folder (for example, `forest_ambience_loop.mp3`).

This repo includes placeholder entries in `audio_assets.json` — replace with real assets and ensure you have the appropriate licenses.
